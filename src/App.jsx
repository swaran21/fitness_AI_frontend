import { Button } from "@mui/material"
import { BrowserRouter as Router,Navigate,Route,Routes,useLocation } from "react-router"

function App() {
  return (
    <Router>  
        <Button variant="contained" color="#dc004r">Login</Button>
    </Router>
  )
}

export default App
